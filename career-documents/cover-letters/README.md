# Cover letters

FR-007 cover letter drafts for owner review.

- `generated/` — Markdown, HTML, CoverLetter JSON, and plan JSON
  (path is covered by `career-documents/**/generated/` in `.gitignore`)
- Do not submit or email without owner review
- Re-run drafts with `scripts/run_cover_letter_manual.py` as needed for applications
